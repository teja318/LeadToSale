<% if @prospect.errors.any? %>

$('#errorMsg').html("<%= escape_javascript render 'errors' %>");
<%else %>

$('#new_prospect')[0].reset();
$('#prospectList').append("<%= escape_javascript render 'prospect' %>");
<%end%>

